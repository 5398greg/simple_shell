#include <sdio.h>
/**
 * main - Entry point
 *
 * Description: 'for adding two number'
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int a, b;

	a = 20;
	b = 30;
	c = a + b
	write(STD_OUT_FILENO, c, 2);

	return (0);
}
